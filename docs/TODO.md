1. currently it only switches when u drop the component inside the view to be the child of that view
2. make the added component be able to be the child of the view component when you drop it inside the view component 
